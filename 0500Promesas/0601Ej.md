# 0601Ej.js — Debug Report

**2 bugs found and fixed.**

### Bug 1 — Missing `return` statement (line 12 → crash)

`return miProme` was placed **inside** the `new Promise(...)` executor callback, not in `recuperarArch`. The function implicitly returned `undefined`:

> `TypeError: Cannot read properties of undefined (reading 'then')`

**Fix:** Move `return` to the outer function so `recuperarArch()` actually returns the promise:
```js
function recuperarArch(nom) {
  return new Promise((resolve, reject) => {  // <-- return here
    ...
  });  // removed the inner `return miProme`
}
```

### Bug 2 — Guard-order logic error (line 6)

`nom.trim() === ''` was evaluated **before** `!nom`. If `nom` is `null` or `undefined`, `nom.trim()` throws:

> `TypeError: Cannot read properties of null (reading 'trim')`

**Fix:** Check `!nom` first (short-circuit), then `trim()`:
```js
if (!nom || nom.trim() === '') {
```
