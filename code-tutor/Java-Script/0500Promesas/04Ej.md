# 04Ej.js — Debug Report

**Bug found — Runtime error on line 17.**

`generarCadenas()` is called **without arguments**, but the function expects `v1` and `v2` as parameters (line 5). Inside the function, `v1` and `v2` will be `undefined`, so `v1[num1]` throws:

> `TypeError: Cannot read properties of undefined (reading '...')`

**Fix** — pass the global arrays as arguments:

```js
const vector = generarCadenas(v1, v2);
```

**Alternatively**, remove the parameters from the function definition and use the global `v1`/`v2` directly, but passing them is cleaner since the globals are already defined.

Everything else is fine — the promise logic, `.then`/`.catch`, and comparison are all correct.
