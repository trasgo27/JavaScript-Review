# 05Ej.js — Debug Report

**No runtime errors.** The script executes without exceptions.

The code performs four tasks correctly:
1. **Filter indie** (line 9-15) — filters `tipo === 'indie'`, prints via `console.table`
2. **Sort by name** (line 17-24) — copies array, sorts by `nombre`, prints
3. **Sort by group** (line 27-34) — copies array, sorts by `grupo`, prints
4. **Filter pre-70s** (line 38-44) — filters `año < 1970`, prints

**Minor observation:** `.join('\n')` converts each filtered/sorted result to a flat string (lines 13, 22, 32, 42), then `console.table()` is called on those strings. `console.table()` is designed for arrays/objects, not single strings. In Node.js this still displays the content, but in browsers it would show a character-indexed table instead of the data rows. The intended output was likely achieved with `console.log()` rather than `console.table()` after joining. Not a crash — just a presentation mismatch.
