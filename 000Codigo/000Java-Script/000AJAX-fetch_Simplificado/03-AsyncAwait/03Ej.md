# Ejercicio 03: Debug - try/catch con async/await

## Estado: 2 de 4 tareas completadas incorrectamente

| Tarea | Estado | Problema |
|-------|--------|----------|
| **Tarea 1** | ❌ Vacía | No se implementó — solo hay un comentario |
| **Tarea 2** | ⚠️ Mal ubicada | El código está en Tarea 2 pero debería estar en Tarea 1 |
| **Tarea 3** | ❌ Vacía | No se implementó |
| **Tarea 4** | ✅ Correcta | try/catch/finally bien estructurado |

---

## Bugs Encontrados

### Bug 1: Tarea 1 vacía (línea 20)

```javascript
/* Tu código aquí */
//El try{} no tiene la misma forma que .then()
```

**Problema**: La tarea pide envolver fetch en try/catch, pero no hay implementación.

---

### Bug 2: Código en Tarea 2 en vez de Tarea 1 (líneas 38-45)

```javascript
try{
    const respuesta     = await fetch(`https://jsonplaceholder.typicode.com/posts/1NOVALIDA`);
    const respuestaJson = await respuesta.json();
    console.table(respuestaJson);
  }catch (error) {
    console.log('Error al obtener datos:', error.message);
  }
```

**Problema**: 
1. Este código pertenece a **Tarea 1**, no Tarea 2
2. La URL es inválida (`/posts/1NOVALIDA`) — esto es para **Tarea 3**
3. **Tarea 2** debería tener una URL válida para mostrar éxito, y una inválida para mostrar error

---

### Bug 3: Tarea 3 completamente vacía (línea 65)

```javascript
/* Tu código aquí */
```

**Problema**: Falta implementar la prueba con URL inválida.

---

## Código Corregido

```javascript
console.log('=== Ejercicio 03: try/catch con async/await ===');

async function hacerEj() {
  // TAREA 1: Fetch exitoso con try/catch
  console.log('\n--- Tarea 1: Fetch en try ---');
  try {
    const respuesta = await fetch('https://jsonplaceholder.typicode.com/users/1');
    const datos = await respuesta.json();
    console.log('Éxito:', datos.name);
  } catch (error) {
    console.log('Error:', error.message);
  }

  // TAREA 2: Error amigable
  console.log('\n--- Tarea 2: Error amigable ---');
  try {
    const respuesta = await fetch('https://jsonplaceholder.typicode.com/users/1');
    const datos = await respuesta.json();
    console.log('Nombre:', datos.name);
  } catch (error) {
    console.log('Error al obtener datos:', error.message);
  }

  // TAREA 3: URL inválida
  console.log('\n--- Tarea 3: URL inválida ---');
  try {
    const resp = await fetch('https://jsonplaceholder.typicode.com/esta URL NO existe');
    console.log('¿Llegamos aquí? No debería...');
  } catch (error) {
    console.log('¡Catch funcionó! Error:', error.message);
  }

  // TAREA 4: finally
  console.log('\n--- Tarea 4: finally ---');
  try {
    const resp = await fetch('https://jsonplaceholder.typicode.com/users/1');
    const datos = await resp.json();
    console.log('Nombre:', datos.name);
  } catch (error) {
    console.log('Error:', error.message);
  } finally {
    console.log('✅ Operación finalizada (finally siempre se ejecuta)');
  }
}

hacerEj();
```

---

## Conceptos Clave

| Estructura | Uso |
|------------|-----|
| `try { }` | Código que puede fallar |
| `catch (error) }` | Manejo del error |
| `finally { }` | Siempre se ejecuta (éxito o error) |

### Flujo de ejecución:

```javascript
try {
  // 1. Se ejecuta primero
  const datos = await fetch(url);
  // 2. Si fetch falla, salta al catch
} catch (error) {
  // 3. Se ejecuta solo si hay error
} finally {
  // 4. SIEMPRE se ejecuta
}
```
